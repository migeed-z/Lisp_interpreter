class pair:

    def __init__(self, left, right):
        self.left = left
        self.right = right

    #if0 e-test e-then e-else : if e-test evaluates to 0, then evaluate e-then; otherwise evaluate e-else

    def if0(self, e_then, e_else):
        pass